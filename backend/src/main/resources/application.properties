# Application
spring.application.name=feathered-oof-bird
server.port=8080

# Auth0 Resource Server
spring.security.oauth2.resourceserver.jwt.issuer-uri=${AUTH0_ISSUER_URI}
auth0.audience=${AUTH0_AUDIENCE}

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5532/ooftish_postgres
spring.datasource.username=ooftish-postgres
spring.datasource.password=Y0uYuM40D3Q14nN140!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/paypal_clone
spring.data.mongodb.auto-index-creation=true

# Redis/Valkey Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.type=redis

# Spring AI Configuration (OpenAI)
# spring.ai.openai.api-key=${OPENAI_API_KEY}
# spring.ai.openai.chat.options.model=gpt-3.5-turbo
# spring.ai.openai.chat.options.temperature=0.7

# Logging
logging.level.com.paypalclone.featheredoofbird=DEBUG
logging.level.org.springframework.web=INFO
