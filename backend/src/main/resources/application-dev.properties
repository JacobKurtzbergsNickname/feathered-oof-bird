# Dev profile disables JWT and uses basic auth
spring.security.oauth2.resourceserver.jwt.issuer-uri=

# Remove spring.profiles.active=dev (not allowed in profile-specific files)

# You can override DB or other settings here if needed
# Example:
# spring.datasource.url=jdbc:postgresql://localhost:5432/ooftish-db
# spring.datasource.username=devuser
# spring.datasource.password=devpass